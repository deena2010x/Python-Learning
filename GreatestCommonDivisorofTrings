class Solution:
    def gcdOfStrings(self, str1: str, str2: str) -> str:
        if str1+str2!=str2+str1:
            return ""
        length1=len(str1)
        length2=len(str2)
        length=math.gcd(length1,length2)
        str3=str1[:length]
        return str3
