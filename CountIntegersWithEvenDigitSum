class Solution:
    def countEven(self, num: int) -> int:
        count=0
        for i in range(num+1):
            string=str(i)
            sum1=0
            for j in string:
                sum1+=int(j)
            if sum1%2==0:
                count+=1
        return count-1
