class Solution:
    def minimumOperations(self, nums: List[int]) -> int:
        set1=set()
        for i in nums:
            if i>0:
                set1.add(i)
        length=len(set1)
        return length
