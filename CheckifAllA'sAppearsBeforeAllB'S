class Solution:
    def checkString(self, s: str) -> bool:
        length=len(s)
        for i in range(1,length):
            if ord(s[i])<ord(s[i-1]):
                return False
        return True
