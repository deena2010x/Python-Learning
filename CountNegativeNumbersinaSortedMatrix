class Solution:
    def countNegatives(self, grid: List[List[int]]) -> int:
        length=len(grid)
        length1=len(grid[0])
        count=0
        for i in range(length):
            for j in range(length1):
                if grid[i][j]<0:
                    count+=1
        return count
