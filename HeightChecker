class Solution:
    def heightChecker(self, heights: List[int]) -> int:
        length=len(heights)
        arr=sorted(heights)
        count=0
        for i in range(length):
            if heights[i]!=arr[i]:
                count+=1
        return count
