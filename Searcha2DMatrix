class Solution:
    def searchMatrix(self, matrix: List[List[int]], target: int) -> bool:
        arr=[]
        for i in matrix:
            for j in i:
                arr.append(j)
        if target in arr:
            return True
        return False
