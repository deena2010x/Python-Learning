class Solution:
    def divideArray(self, nums: List[int]) -> bool:
        arr=collections.Counter(nums)
        for i in arr.values():
            if i%2!=0:
                return False
        return True
