class Solution:
    def findNumbers(self, nums: List[int]) -> int:
        length=len(nums)
        count=0
        for i in range(length):
            string=str(nums[i])
            length1=len(string)
            if length1%2==0:
                count+=1
        return count
