class Solution:
    def hammingWeight(self, n: int) -> int:
        binary=bin(n)
        string=str(binary)
        count=0
        for i in string:
            if i=='1':
                count+=1
        return count
