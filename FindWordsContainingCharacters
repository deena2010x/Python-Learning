class Solution:
    def findWordsContaining(self, words: List[str], x: str) -> List[int]:
        arr=[]
        length=len(words)
        for i in range(length):
            if x in words[i]:
                arr.append(i)
        return arr
