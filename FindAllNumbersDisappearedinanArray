class Solution:
    def findDisappearedNumbers(self, nums: List[int]) -> List[int]:
        length=len(nums)
        arr=[]
        for i in range(1,length+1):
            arr.append(i)
        set1=set(arr)
        set2=set(nums)
        set3=set1-set2
        arr1=list(set3)
        return arr1
